export const isUserAuthenticated = (): boolean => {
  // Example logic to check authentication status (replace with your actual logic)
  return true; // Assume user is authenticated for demonstration
};